[tool.uv.workspace]
members = ["packages/semduck"]

[dependency-groups]
dev = [
  "pytest",
  "tox>=4.24",
  "dbt-duckdb>=1.9,<1.11",
  "numpy>=2.4.4",
]

[tool.uv.sources]
semduck = { workspace = true }
